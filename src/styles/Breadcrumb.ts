import type { BreadcrumbProps, BreadcrumbVariants } from '../components/interface'
import type { StylerStyles } from '../context'
import { cssvar } from '../util'

export const BreadcrumbStyles: StylerStyles<BreadcrumbProps, BreadcrumbVariants> = {
  bps: {},
  colors: {},
  base: {},
  variants: {},
}
