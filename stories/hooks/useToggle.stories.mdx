import { Meta, Story, Canvas } from "@storybook/addon-docs";

<Meta title="Hooks/useToggle" />

# useToggle

Returns a tuple containing:

* A boolean state.
* A function to toggle that state.
* A function to set the state explicity.

## Example

```tsx
import { useToggle } from '@locktech/atomic'

export const MyComponent = () => {
  const [open, toggleOpen, setOpen] = useToggle(false)

  return ...
}
```

## Parameters

| Property     	| Required     	| Default     	| Description                                                                                                                                  	|
|--------------	|--------------	|--------------	|----------------------------------------------------------	 |
| `initial`    	| `true`      	| `false`      	| The initial value of the toggle when it is first returned. |

