import { Meta, Story, Canvas } from "@storybook/addon-docs";

<Meta title="Hooks/useToggle" />

## useToggle

Returns a boolean state and a function to toggle that state's value each time the function is invoked.
Optionally, a value can be provided to the function — which will be used to set the state's current value.

```tsx
import { useToggle } from '@locktech/atomic'

export const MyComponent = () => {
  const [open, toggleOpen] = useToggle()

  return ...
}
```
